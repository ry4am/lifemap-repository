// prisma/schema.prisma
// Accounts database: users + auth

datasource db {
  provider = "postgresql"
  // Neon connection string for lifemap-accounts
  url      = env("DATABASE_URL_ACCOUNTS")
}

generator client {
  provider = "prisma-client-js"
}

// Basic user model + fields you need for login
model User {
  id           String   @id @default(cuid())
  name         String?
  email        String   @unique
  passwordHash String
  role         String   @default("participant")
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  // Appointments live in a separate DB; we reference userId there as a String
}

// The 3 models below are required if you’re using NextAuth + PrismaAdapter

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String

  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}
